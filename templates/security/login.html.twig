{% extends 'base.html.twig' %}

{% block title %}
    Connexion
{% endblock %}

{% block header %}

{% endblock %}

{% block content %}
    <div style="background-color: red " >
        <div style="background-size: cover">

<div style="text-align:center">
   <h1 >Connexion</h1>
</div>
    <form action="{{ path('connexion') }}" method="post">
        <div class="form-group">
            <input  placeholder="username" name="_username" value="{{ last_id }}"
                   type="text" class="form-control">

            <input placeholder="password"  name="_password"
                   type="password" class="form-control">
            {% if error %}
            <span style="color:#ff0000"  >Veuillez verifier vos donnees !! </span>
        {% endif %}
        </div>

        <div class="form-group">
            <button type="submit" class="btn btn-success">Connexion</button>
        </div>
    </form>
    </div>
    </div>

{% endblock %}

{% block footer %}

{% endblock %}
